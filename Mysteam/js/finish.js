/*最终购买输出*/
$(function(){
  
    $(".add_to_cart_button?").click(function () {
	    /*var pro_name = $("#productName").text();
	    var pro_num = $("#productnum").val();
		var pro_price = $("#Nowprice").text();
	    
	    $.post(
            "handlers/CartHandler.ashx",
            { id: pro_id, name: pro_name, price: pro_price, quantity: pro_num },
            function (data) {
                if (data != "error") {
                    //处理界面上购物车数量
                    alert("您已经下单! 产品编号" + data);
                } else {
                    alert("下单失败!");
                }
            }
        );

		return false;*/
        alert('s');
	})
})
